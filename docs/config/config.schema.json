{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/bitwise-media-group/evolve/main/docs/config/config.schema.json",
  "title": "evolve configuration",
  "description": "Optional .evolve.<ext> configuration for an evolve repository (yaml, yml, json, or jsonc at the root; at most one). Generated from the viper config by `evolve docs --format config`; do not edit by hand.",
  "type": "object",
  "properties": {
    "$schema": {
      "description": "Optional URI of this schema; ignored by evolve, honored by editors.",
      "type": "string"
    },
    "version": {
      "description": "Terraform-style semver constraint (\"0.4.0\", \"~> 0.4\", \">= 0.4, < 1\") the evolve binary must satisfy before run triggers/evals/all or report will rewrite results. Non-release builds (dev and snapshot/prerelease versions) warn and skip the check.",
      "type": "string"
    },
    "layout": {
      "description": "Repository layout: auto, marketplace, multi, or single.",
      "type": "string",
      "enum": [
        "auto",
        "marketplace",
        "multi",
        "single"
      ],
      "default": "auto"
    },
    "models": {
      "description": "Restriction on which models exist: provider ids, canonical model ids (anthropic/claude-sonnet-4-6), or all. Unlisted models are unavailable. --model filters within it.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "harnesses": {
      "description": "Restriction on which agent CLIs (claude, codex, gemini, cursor, copilot, antigravity) may drive models. --harness filters within it.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "cache_dir": {
      "description": "Directory holding the token-count cache.",
      "type": "string"
    },
    "results_format": {
      "description": "Format for committed results files and the EVALUATION rollup: json, jsonc, or yaml.",
      "type": "string",
      "enum": [
        "json",
        "jsonc",
        "yaml"
      ],
      "default": "json"
    },
    "telemetry": {
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "dir": {
          "description": "Directory for the OpenTelemetry JSON exporter (traces.json, metrics.json, logs.json); the --telemetry-dir flag overrides it and both win over OTEL_* env vars.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "max_turns": {
      "description": "Default maximum agent turns per behavioral eval; --max-turns and a per-eval max_turns override it.",
      "type": "integer",
      "default": 20
    },
    "baseline": {
      "description": "Benchmark each eval without the skill (the skill's lift over no skill), recomputed only when the eval or its fixtures change. --baseline overrides for one run.",
      "type": "boolean",
      "default": true
    },
    "stale_results": {
      "description": "How run/report treat stored results for models outside the `models` restriction: keep or drop. --stale-results overrides.",
      "type": "string"
    },
    "sandbox": {
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "enabled": {
          "description": "Confine agent writes with an OS sandbox (sandbox-exec on macOS, bubblewrap on Linux); --no-sandbox overrides for one run.",
          "type": "boolean",
          "default": true
        },
        "protected_roots": {
          "description": "Directories kept read-only to agent runs so an escaping agent cannot modify other source repositories; the workspace stays writable. Reads, the network, and tool caches outside these roots are unaffected.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "checks": {
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "license": {
          "description": "License every SKILL.md must declare; when unset, skills must not declare one.",
          "type": "string"
        },
        "description_pattern": {
          "description": "Regex every skill description must match.",
          "type": "string",
          "default": "Use (when|after|before)"
        },
        "max_skill_lines": {
          "description": "Maximum SKILL.md line count.",
          "type": "integer",
          "default": 500
        },
        "ideal_skill_lines": {
          "description": "Ideal SKILL.md line count for the advisory size signal (full at or below; zero at the cap).",
          "type": "integer",
          "default": 200
        },
        "signals": {
          "description": "Emit the advisory skill-quality signals after run checks; the --no-signals flag forces them off.",
          "type": "boolean",
          "default": true
        },
        "plugin_manifests": {
          "description": "Plugin manifests every plugin must ship: claude (.claude-plugin/plugin.json) and/or codex (.codex-plugin/plugin.json). With both, a hooks/ directory is forbidden (codex and claude hooks.json are incompatible).",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "claude",
            "codex"
          ]
        },
        "marketplace": {
          "description": "Validate marketplace manifests (marketplace layout only).",
          "type": "boolean",
          "default": true
        }
      },
      "additionalProperties": false
    },
    "report": {
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "thresholds": {
          "type": [
            "object",
            "null"
          ],
          "properties": {
            "triggers_min_pass_rate": {
              "description": "Minimum triggers pass rate (0-1); report --check exits 1 below it and the run dashboard rolls a failing group to an orange check while its rate stays at or above it.",
              "type": "number",
              "minimum": 0,
              "maximum": 1,
              "default": 0.5
            },
            "evals_min_pass_rate": {
              "description": "Minimum evals pass rate (0-1); report --check exits 1 below it and the run dashboard rolls a failing group to an orange check while its rate stays at or above it.",
              "type": "number",
              "minimum": 0,
              "maximum": 1,
              "default": 0.66
            },
            "models": {
              "description": "Model keys (provider/model-id) the thresholds apply to.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "maturity": {
              "description": "Plugin maturity levels whose Tier 1/Tier 2 evidence issues fail report --check; other levels only warn (evidence still renders). A plugin's maturity comes from its manifest version: stable (>= 1.0.0), unstable (< 1.0.0), or prerelease (a SemVer prerelease tag). The --maturity flag overrides it.",
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "stable",
                  "unstable",
                  "prerelease"
                ]
              },
              "default": [
                "stable",
                "unstable",
                "prerelease"
              ]
            }
          },
          "additionalProperties": false
        },
        "junit": {
          "description": "Path for a JUnit XML test-results file (one testcase per eval/trigger case per model); the --junit flag overrides it.",
          "type": "string"
        },
        "cobertura": {
          "description": "Path for a Cobertura XML coverage file marking each skill covered by a current eval result; the --cobertura flag overrides it.",
          "type": "string"
        },
        "strict": {
          "description": "Require the configured model matrix: report --check holds every defined model to the thresholds, and the Cobertura output covers a skill only when every defined model has a current result. The --strict flag overrides it.",
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false
    },
    "providers": {
      "description": "Per-provider overrides: providers.<name>.models replaces that provider's builtin model matrix (model ids, display names, USD-per-mtok pricing).",
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "models": {
            "description": "Replacement model matrix for this provider.",
            "type": "array",
            "items": {
              "$ref": "#/$defs/model"
            }
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false,
  "$defs": {
    "model": {
      "description": "One model in a provider's matrix.",
      "type": "object",
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "description": "Model id passed to the runner CLI (required).",
          "type": "string",
          "minLength": 1
        },
        "display": {
          "description": "Human-readable name shown in reports (default: the id).",
          "type": "string"
        },
        "input_per_mtok": {
          "description": "Input price in USD per million tokens (omit when unpublished).",
          "type": "number"
        },
        "output_per_mtok": {
          "description": "Output price in USD per million tokens (omit when unpublished).",
          "type": "number"
        }
      },
      "additionalProperties": false
    }
  }
}
